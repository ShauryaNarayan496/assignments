# this code is importing the math_utils module and using the functions to calculate the square, cube and check if a number is even 
import math_utils
num = int(input())
print(math_utils.square(num))
print(math_utils.cube(num))
print(math_utils.number_is_even(num))